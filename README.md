# Lazy-recon
A simple bash script that automates the boring parts of web recon. You give it a domain, it does subdomain enumeration, port scanning, screenshots, vulnerability scanning, crawling, and directory brute-forcing — all at once. Everything runs in parallel so you're not sitting there waiting for each tool to finish one by one.
